--- requirements.txt (原始)


+++ requirements.txt (修改后)
# Зависимости для приложения "Напоминалка"
# Установите командой: pip install -r requirements.txt

tkinter  # Встроен в Python (GUI интерфейс)
pygame>=2.5.0  # Для воспроизведения звука
pyaudio>=0.2.13  # Для записи голоса
numpy>=1.24.0  # Для генерации звуковых сигналов